export interface Box {
    img: string;
    alt: string;
    header: string;
    body: string;
    id: string;
}

export interface Video {
    title: string;
    src: string;
    embed: boolean;
    poster: string;
}